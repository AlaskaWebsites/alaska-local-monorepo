# ==============================================================================
# 🏔️ ALASKA LOCAL — BACKEND ENVIRONMENT CONFIGURATION (apps/api/.env.example)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. SERVIDOR HTTP (NestJS 11)
# ------------------------------------------------------------------------------
NODE_ENV=development
PORT=3333
CORS_ORIGINS=*

# ------------------------------------------------------------------------------
# 2. BANCO DE DADOS (PostgreSQL 16)
# Conexão padrão local configurada no docker-compose.yml e docker/init.sql
# ------------------------------------------------------------------------------
DATABASE_URL=postgres://alaska:alaskapassword@localhost:5432/alaska_local

# ------------------------------------------------------------------------------
# 3. FILAS ASSÍNCRONAS & CACHE (Redis 7)
# Conexão padrão local do Redis no Docker (porta 6379)
# ------------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# ------------------------------------------------------------------------------
# 4. SUPABASE (Opcional - Conexão em Nuvem para Produção/Staging)
# Preencha se preferir conectar diretamente ao banco hospedado na Supabase
# ------------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_KEY=

# ------------------------------------------------------------------------------
# 5. ASAAS PAGAMENTOS (Opcional - Webhooks e Gestão de Cobranças Pix)
# Chave de API do Asaas para ambiente Sandbox ou Produção
# ------------------------------------------------------------------------------
ASAAS_API_KEY=
